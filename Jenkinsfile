pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Start"'
                sh 'sleep 15'
                sh 'echo "End"'
            }
        }
    }
}
