pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Start"'
                sh 'sleep 5'
                sh 'echo "End"'
            }
        }
    }
}
